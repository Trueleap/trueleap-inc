# Payload CMS URL
PAYLOAD_URL=http://localhost:3000

# Payload API key (optional — only needed if access.read is restricted)
PAYLOAD_API_KEY=

# Set to "true" on the preview site to fetch draft content (SSR mode)
# DRAFT_MODE=true
