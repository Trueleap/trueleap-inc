# Keystatic session signing key (openssl rand -hex 32)
KEYSTATIC_SECRET=

# GitHub PAT — used by Keystatic (content editing) + merge dashboard
# Fine-grained PAT scoped to repo with contents:write + pull_requests:write
GITHUB_TOKEN=
