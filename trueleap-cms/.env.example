# Generate with: openssl rand -hex 32
PAYLOAD_SECRET=

# GitHub PAT for deploy trigger + workflow status polling
# Fine-grained PAT scoped to repo with: contents:write + actions:read
GITHUB_TOKEN=

# GitHub repo (owner/name)
GITHUB_REPO=Trueleap/trueleap-inc

# Frontend URLs (used for preview links in admin)
FRONTEND_URL=http://localhost:4321
PREVIEW_URL=http://localhost:4322
